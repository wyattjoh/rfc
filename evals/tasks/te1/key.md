# te1 — issued token type

1. Yes. `issued_token_type` is REQUIRED in a successful response and identifies the representation of the issued security token — **RFC 8693 §2.2.1**, L442–L444.

Note: This is distinct from the REQUIRED `token_type`, which specifies how the issued token is used (L446–L452).
